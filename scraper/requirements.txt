# Stage 1-2 dependencies
requests>=2.31
pdfplumber>=0.11        # PDF text + table extraction (pulls pdfminer.six)
beautifulsoup4>=4.12    # HTML parsing
lxml>=5.0               # fast HTML/XML parser backend

# OCR for scanned PDFs (stage 2 fallback). Pure-Python here, but ALSO needs the
# system binaries `tesseract` and `poppler` (pdftoppm) on PATH. If those are
# missing, the extractor flags the doc as scanned/needs_review instead of crashing.
pytesseract>=0.3
pdf2image>=1.17
Pillow>=10.0
